services:
  - type: web
    name: fer-proj-api
    env: python
    plan: free
    region: oregon


    buildCommand: pip install -r requirements.txt -r requirements-dev.txt

    startCommand:

    envVars:
